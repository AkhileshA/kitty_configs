# Linux-specific overrides. Selected by switch-theme.sh via the
# configs/platform.conf symlink, and included after the active theme so
# this font_family wins. Keeping the font here (instead of in every theme)
# lets the same theme files work unchanged on both Linux and macOS.
font_family Adwaita Mono
